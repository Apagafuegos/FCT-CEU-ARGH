package proyectoFront.gui;

import javafx.fxml.FXML;
import javafx.scene.layout.AnchorPane;
import javafx.scene.shape.Rectangle;

public class BienvenidaController extends AppController{

	@FXML
	private AnchorPane myAnchorPane;

	@FXML
	public void initialize() {
	    // Crear un Rectangle con las mismas dimensiones que el AnchorPane
	    Rectangle clip = new Rectangle(myAnchorPane.getWidth(), myAnchorPane.getHeight());
	    clip.setArcWidth(30); // Ajusta el radio de las esquinas
	    clip.setArcHeight(30);

	    // Aplicar el clip al AnchorPane
	    myAnchorPane.setClip(clip);
	}

}
